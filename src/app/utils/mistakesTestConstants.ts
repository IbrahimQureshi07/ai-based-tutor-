/** Stage 2.5 — Mistakes test (same length target as Stage 2 prep). */
export const MISTAKES_TEST_TOTAL = 110;
/** Target slots filled from past S1+S2 medium/hard wrong bank questions (unique IDs). */
export const MISTAKES_TEST_MISTAKE_SLOTS_TARGET = 80;
/** If total `hard_wrong` outcome rows (S1+S2) exceed this count, mistake pool uses hard-only questions. */
export const MISTAKES_TEST_HARD_WRONG_ROW_THRESHOLD = 70;
